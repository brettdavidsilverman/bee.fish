#ifndef BEE_FISH__SIZE
#define BEE_FISH__SIZE

typedef size_t Size;
typedef ssize_t SSize;
typedef size_t Index;

inline static const size_t
    SESSION_ID_SIZE = 32;

inline static const size_t
    USER_ID_SIZE = 32;
    
#endif